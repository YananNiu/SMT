"""SMT: Sky-image Multimodal Transformer for short-term solar (GHI) forecasting.

Public entry points:
    smt.engine.make_vilt / run_model   -- build + train a model from an args namespace
    smt.data_provider.DataGenerator_ViLT
    smt.config.load_config             -- YAML -> args (SimpleNamespace)
"""
